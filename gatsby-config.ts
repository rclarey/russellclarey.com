import type { GatsbyConfig } from "gatsby";

const config: GatsbyConfig = {
  siteMetadata: {
    title: `Russell Clarey`,
    siteUrl: `https://russellclarey.com`,
  },
  graphqlTypegen: true,
};

export default config;
